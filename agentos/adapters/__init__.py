"""Встроенные адаптеры хранения: платформа полнофункциональна на одной
машине без внешних зависимостей (in-memory, SQLite). Продакшн-адаптеры
(PostgreSQL, Redis, брокеры, векторные БД) подключаются как плагины."""
