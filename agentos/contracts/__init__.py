"""Публичные контракты AgentOS: порты, события, DTO.

Правило зависимостей: contracts не зависят ни от kernel, ни от runtimes —
только стандартная библиотека. Модули импортируют чужие контракты,
но никогда — чужие реализации.
"""
